{
  "name": "pf2e-market-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PF2E Market Forge with hardened contracts, requester-bound GM checkout sessions, shared product resolution, and diagnostics",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
